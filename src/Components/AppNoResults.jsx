// Component: AppNoResults
// Purpose: simple placeholder shown when no items are available.
// No props. Keep this minimal and reusable.
export default function AppNoResults () {
    return(

        <div className="col col-12">
            <h1>Nessun risultato</h1>
        </div>
    )
}